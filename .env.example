NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Recommended: lets Crossref identify requests and use its polite pool.
CROSSREF_MAILTO=you@example.com

# Optional fallback for exact-title lookup. The app works without it.
OPENALEX_API_KEY=
